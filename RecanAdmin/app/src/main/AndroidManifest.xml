<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acerspin5.myapplication">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_admin_square_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_admin"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Barcode_qr_Scan"
            android:theme="@style/reward_hr" />
        <activity
            android:name=".ScanCodeActivity"
            android:theme="@style/reward_hr" />
        <activity android:name=".User_detail"></activity>
    </application>

</manifest>